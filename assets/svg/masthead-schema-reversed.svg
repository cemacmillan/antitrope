<svg width="480" height="480" viewBox="0 0 480 480" xmlns="http://www.w3.org/2000/svg">

  <!-- COLOR PALETTE: Schema-based Reversed (from base.css) -->
  <!-- Background: Night Opium (#1B201C) | Foreground: Aged Silk (#E6DFCC) -->
  <!-- Outer circle radius: 199.25 for R kern clearance -->

  <defs>
    <style type="text/css">
      .outer-text {
        font-family: "Noto Serif", "EB Garamond", serif;
        font-size: 26px;
        letter-spacing: 3px;
        fill: #E6DFCC;
      }
      .inner-text {
        font-family: "Noto Sans Phoenician", "Courier New", monospace;
        font-size: 26px;
        letter-spacing: 2.5px;
        fill: #E6DFCC;
      }
      .tri-radial {
        stroke: #E6DFCC;
        stroke-width: 8;
        stroke-linecap: round;
      }
    </style>
  </defs>

  <!-- BACKGROUND -->
  <rect width="100%" height="100%" fill="#1B201C" />

  <!-- INNER CIRCLE -->
  <circle cx="240" cy="240" r="150" fill="none" stroke="#E6DFCC" stroke-width="3"/>

  <!-- OUTER CIRCLE (Tighter: reduced from 210 to 199.25 for R kern clearance) -->
  <circle cx="240" cy="240" r="199.25" fill="none" stroke="#E6DFCC" stroke-width="4"/>

  <!-- OUTER TEXT PATH (Adjusted for tighter radius) -->
  <defs>
    <path id="outerCircle"
          d="M 40.75,240
             A 199.25,199.25 0 0,1 439.25,240"
          fill="none" />
  </defs>

  <!-- OUTER TEXT -->
  <text class="outer-text">
    <textPath href="#outerCircle" startOffset="50%" text-anchor="middle">
      ΥΠΕΡ ΤΩΝ ΤΕΚΝΩΝ  ·  ANTITROPE  ·  ÉDITIONS
    </textPath>
  </text>

  <!-- INNER TEXT PATH -->
  <defs>
    <path id="innerCircle"
          d="M 240,90
             A 150,150 0 1,1 239.999,90"
          fill="none" />
  </defs>

  <!-- INNER TEXT -->
  <text class="inner-text">
    <textPath href="#innerCircle" startOffset="50%" text-anchor="middle">
      𐤅𐤐𐤄𐤓   𐤕𐤅𐤍   𐤕𐤄𐤊𐤍𐤅𐤍
    </textPath>
  </text>

  <!-- CENTRAL TRI-RADIAL DEVICE -->
  <!-- Geometric, non-rotational, triadic equilibrium symbol -->
  <line x1="240" y1="200" x2="240" y2="140" class="tri-radial"/>
  <line x1="240" y1="200" x2="195" y2="260" class="tri-radial"/>
  <line x1="240" y1="200" x2="285" y2="260" class="tri-radial"/>

</svg>

